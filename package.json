{
  "name": "lithium-ts-stater",
  "version": "1.0.0",
  "source": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clear:dist": "rm -rf dist",
    "clear:cache": "rm -rf .parcel-cache",
    "clear": "npm run clear:dist & npm run clear:cache",
    "start": "npm run clear && parcel --port 8080 --log-level verbose",
    "build": "npm run clear && parcel build --log-level verbose"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@lithium-framework/core": "file:../../core",
    "@microsoft/fast-element": "^2.0.0-beta.26"
  },
  "devDependencies": {
    "@babel/core": "^7.24.9",
    "@babel/plugin-proposal-class-properties": "^7.18.6",
    "@babel/plugin-proposal-decorators": "^7.24.7",
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "typescript": "^5.5.4"
  }
}
